import Heading from "../Heading/Heading";

const NoReviews = () => {
  return (
    <div>
      <Heading mainTitle="no reviews yet" subTitle="sign in to add a review" />
    </div>
  );
};

export default NoReviews;
